spring:
  application:
    name: gateway-service
  cloud:
    config:
      uri: http://config-server:8888
      fail-fast: true
      username: user
      password: 1234
  main:
    allow-bean-definition-overriding: true